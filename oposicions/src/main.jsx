import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.jsx';

// The entry point for the opposition study platform.
// This file mirrors the structure of the BusinessEnglish app but
// does not include any domain‑specific logic or content.

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
