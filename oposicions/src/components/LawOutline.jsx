import React from 'react';

// A simple component placeholder for displaying a structured outline of a law.
// Actual legal content should be supplied separately via data files.

function LawOutline({ title }) {
  return (
    <div className="law-outline">
      <h2>{title}</h2>
      {/* Outline content will be injected here in the future */}
    </div>
  );
}

export default LawOutline;
