# Environment variables (template)
#
# Copy this file to .env and fill in the values.
# The real .env file is excluded from Git via .gitignore.
#
# Currently the app does not require any environment variables.
# This file exists as a convention for future use.

# VITE_EXAMPLE_KEY=your-value-here
